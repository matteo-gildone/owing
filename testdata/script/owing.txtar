# With path but no flags, show output
exec owing ./testdata
stdout 'nested-folder/test-file.js:1 \[TODO\] Add handle of edge cases'
stdout 'nested-folder/test-file.js:6 \[HACK\] Temporary random ID generator \(not collision-safe\)'
stdout 'nested-folder/test-file.js:11 \[TODO\] Replace with proper validation logic'
stdout 'nested-folder/test-file.js:17 \[NOTE\] Only checking name for now'
stdout 'test-file.js:1 \[FIXME\] this is wrong'

-- testdata/test-file.js --
// FIXME: this is wrong
export function add (a, b) {
  return a - b;
}

-- testdata/nested-folder/test-file.js --
// TODO: Add handle of edge cases
export function diff (a, b) {
    return a - b;
}

// HACK: Temporary random ID generator (not collision-safe)
export function generateID () {
    return Math.floor(Math.random() * 1000);
}

// TODO: Replace with proper validation logic
export function isValidUser(user) {
    if (!user) {
        return false
    }

    // NOTE: Only checking name for now
    return typeof user.name === 'string';
}
